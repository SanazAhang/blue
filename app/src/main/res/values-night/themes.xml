<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Blue" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->
        <item name="android:colorBackground">@color/black</item>
        <item name="android:windowTranslucentStatus">true</item>
        <item name="android:windowTranslucentNavigation">true</item>
        <item name="backDropImage">@drawable/header_dark</item>
        <item name="iconColor">@color/header_section_dark</item>
        <item name="headerTextColor">@color/header_section_dark</item>
        <item name="frontBackgroundColor">@color/black</item>

    </style>
</resources>